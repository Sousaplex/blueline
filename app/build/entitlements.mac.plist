<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Blueline runs the engine bridge as a child of the packaged app via
       ELECTRON_RUN_AS_NODE, and launches Playwright's Chromium. Under a hardened
       runtime those need the JIT / unsigned-exec-memory exceptions, and library
       validation must be relaxed so the bundled node_modules native addons load. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Child processes inherit the parent's sandbox/runtime entitlements. -->
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
